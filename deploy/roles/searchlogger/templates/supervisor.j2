[program:{{ appname }}]
command={{ venv }}/bin/gunicorn {{ appname }}.wsgi --bind 127.0.0.1:{{ localport }} --pid /tmp/gunicorn-{{ appname }}.pid
directory={{ django_dir }}
environment=DJANGO_SETTINGS_MODULE="{{ django_settings }}"
